import { Login } from "@/features/auth/components/Auth";

export const metadata = {
  title: "Acceder",
  description: "Accede a tu cuenta de KODA.",
};

export default function LoginPage() {
  return (
    <Login />
  );
}
